{
  "name": "claude-talking",
  "version": "1.0.0",
  "description": "Desktop voice assistant for Claude Code - talk to Claude using mic, speaker, and camera.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "desktop": "cd desktop && npx electron .",
    "setup": "bash setup.sh"
  },
  "dependencies": {
    "node-edge-tts": "^1.2.10"
  }
}
